# lak-ruby
Ruby Utils

## Endless Ruby

input

```py
def mine
  if a
    b
  else
    ruby:
      {
         t: Time.now.to_i,
         rand: rand,
      }
```

output:

```
def mine
  if a
    b
  else
    {
       t: Time.now.to_i,
       rand: rand,
    }
  end
end
```
